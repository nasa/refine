dnl Process this file with autoconf to produce a configure script.
AC_CONFIG_AUX_DIR(config)
AC_INIT(src/grid.c)
AM_INIT_AUTOMAKE(grid,0.1)

dnl Checks for std C stuff
AC_PROG_CC
AC_HEADER_STDC
AM_CONFIG_HEADER(config.h)

AC_OUTPUT(Makefile
	  src/Makefile)
